bugfixes:
  - vyos_config - fixed integration test fixture for allow_password_change=all which sent a syntactically invalid encrypted-password value to the device, causing test failures against real hardware/lab targets.
