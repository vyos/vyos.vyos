---
- name: Remove route-maps and VRF configuration
  vyos.vyos.vyos_config:
    lines:
      - delete policy route-map rm1
      - delete vrf
  ignore_errors: true
  vars:
    ansible_connection: ansible.netcommon.network_cli
