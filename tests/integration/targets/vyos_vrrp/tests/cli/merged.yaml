---
- debug:
    msg: START vyos_vrrp merged integration tests on connection={{ ansible_connection }}

- include_tasks: _remove_config.yaml

- include_tasks: _populate_config.yaml

- block:
    - name: Merge the provided configuration with the existing running configuration
      register: result
      vyos.vyos.vyos_vrrp: &id001
        config: "{{ merged.config }}"
        state: merged

    - vyos.vyos.vyos_facts:
        gather_network_resources: vrrp

    - assert:
        that:
          - result.changed == true
          - result.commands | symmetric_difference(merged.commands) == []
          - result.after | symmetric_difference(ansible_facts['network_resources']['vrrp']) == []

    - name: Assert that before dicts were correctly generated
      assert:
        that:
          - merged.before | symmetric_difference(result.before) == []

    - name: Merge the provided configuration with the existing running configuration (IDEMPOTENT)
      register: result
      vyos.vyos.vyos_vrrp: *id001

    - name: Assert that the previous task was idempotent
      assert:
        that:
          - result['changed'] == false
          - result['commands'] == []
          - result['before'] | symmetric_difference(ansible_facts['network_resources']['vrrp']) == []

  always:
    - include_tasks: _remove_config.yaml
