---
- name: Remove pre-existing vrrp config
  vyos.vyos.vyos_vrrp:
    config:
    state: purged
  ignore_errors: true
  vars:
    ansible_connection: ansible.netcommon.network_cli

- name: Remove dummy scripts for VRRP tests
  vyos.vyos.vyos_command:
    commands:
      - rm -f /var/tmp/script.sh
  vars:
    ansible_connection: ansible.netcommon.network_cli
